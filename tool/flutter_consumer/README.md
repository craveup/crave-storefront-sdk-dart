# Flutter consumer fixture

This disposable package proves that the pure-Dart SDK resolves, analyzes, and compiles in a current
Flutter application. It is intentionally not a storefront example or application template.

Run it from this directory with `flutter pub get`, `flutter analyze`, and `flutter test`.
